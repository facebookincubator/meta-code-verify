{
  "name": "fb-code-verify",
  "version": "0.1.0",
  "description": "Browser extensions to verify FB code running in the browser against a published manifest",
  "main": "none",
  "repository": "git@github.com:facebookincubator/fb-code-verify.git",
  "author": "Richard Hansen <rrh@fb.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build-js": "cat src/js/config.js src/js/contentUtils.js src/js/detectWAMeta.js > dist/chrome/contentWA.js",
    "build-local-dev-chrome": "yarn clean && cp config/* dist/chrome && yarn build-js && cp -r src/js/* dist/chrome",
    "build-local-dev": "yarn build-local-dev-chrome",
    "clean": "rm -rf dist/chrome/*"
  }
}
